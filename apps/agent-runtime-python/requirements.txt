# Direct runtime dependencies for the OpenBot Python execution unit.
#
# Both are pinned exactly and reviewed in RESEARCH.md. Transitive resolution is
# frozen in requirements.lock (dev) and requirements-runtime.lock (runtime).
# Install the development closure with the package-local virtualenv only:
#
#   python -m venv .venv
#   ./.venv/bin/pip install -r requirements.lock
#
pydantic-ai-slim==2.47.0
jsonschema-rs==0.57.1
