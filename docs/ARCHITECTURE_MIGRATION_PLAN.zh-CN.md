# 架构迁移分段交付计划

> 2026-09-24 执行出口更新：用户要求完成当前延期审批/unknown 继续执行切片后暂停；
> 不再启动其他 S3 或 S4 工作。这不表示 S3 或整体迁移完成。以当前简短交接为准。

当前切片（父提交 `965a643`）已验收：延期提案/审批在回执丢失后继续、原 Action 的 unknown 核对、
取消后拒绝写入、拒绝终态安全读回。真实 HTTP/PG/mTLS 恢复、下载、回放及原无延期并行路径回归通过。
本切片不表示 S3 完成；按最新要求暂停。见[证据](research/work-deferred-approval.md)。

[English](ARCHITECTURE_MIGRATION_PLAN.md) · [简体中文](ARCHITECTURE_MIGRATION_PLAN.zh-CN.md)

状态：用户重新批准后进行中，2026-09-23。本文件是唯一交付计划。优先考虑长期产品质量，接受更大的迁移工程量；单项阶段完成不等于总目标完成。

对象、恢复、动作结果、预算与下一项完整流程见[工作执行目标契约](WORK_EXECUTION_CONTRACT.zh-CN.md)。

## 本次批准的约束与近期顺序

目标围绕工作重新组织，不是逐文件翻译旧 Server：Bot 是持久身份，Task 是用户的工作目标，Run 是一次执行尝试，Action 是一次具体的受控动作，Artifact 是可独立访问的成果。聊天与频道是交互入口，不拥有任务生命周期。这是职责定义，尚未定死 SQL 模型，也不要求增加服务数量。

Python 后端保持模块边界。Runtime 提议决策；控制层拥有权限、预算、审批与持久结果。成熟工作流组件负责恢复编排，动作回执与核对策略处理外部副作用和本地提交之间的不确定性。引擎重放、工具调用 ID 都不能证明外部写入恰好一次；进程、虚拟环境或容器本身也不等于完成安全隔离。

近期顺序调整为：**S3 恢复方案选型与 S4 隔离设计 → S2/S3 任务完整流程 → S2 其余接口与客户端 → S4 真实工作 → S5/S6/S7 整体验收**。保留旧阶段编号以关联已有证据，不再先翻译完 CRUD。固定 DBOS 与 Temporal 版本，验证杀进程、审批等待、取消、撤权、重复派发和未知外部结果，再选择唯一恢复负责人，不叠加多套独立重试引擎。首个实验与生产实现分离。

2026-09-24 已按[架构决策](decisions/0046-temporal-as-recovery-owner.md)选择 Temporal 为目标实现的唯一恢复负责人。这个决定允许接入产品，但不等于生产默认版本已可启用；生产派发、真实 Runtime 组装、恢复操作、API 授权/PKI 和隔离验收仍是门槛。已关闭的参考工作流现可由独立命令工作流核实历史动作，但不能恢复原 Agent 或自动完成 Task。

已有成果区分为产品不变量、候选实现、过渡设施。保留有效测试和修复；候选代码按行为验收；会话互认与局部后端模式需安排退出。允许为目标行为重新设计内部模型、数据库和 API，但保护原始数据和历史迁移。语言方向仅根据新的实际证据调整，不受沉没成本或竞品选型左右。

WorkBuddy 的 Temporal 实验 Worker 与 Codex 独立故障程序已完成本地验收。官方 SDK 组合八项、公开工作集成八项现已通过。公开流程使用 TemporalDurability 与受信工作流策略，控制层拥有审批/权限/预算，外部回执独立核对，产物验证后提交。交接确认持久且幂等，实验派发器覆盖数据库提交与引擎入队之间的间隙；尚无生产派发器，也未自动连接现有 stdin/stdout Runtime。固定发行版 Server/PostgreSQL 现也通过同样八项流程，含旧引擎备份与新业务事实并存的恢复、引擎与数据库 SIGKILL；这只是单机持久化证据。可选 mTLS 已通过受信客户端拒绝、停机 CA 更换及同样八项流程；等待/完成真实历史重放不改变状态或动作计数，并能识别不兼容命令。下一步仍须验证生产 API 授权/PKI、版本升级和未来代码兼容、未决任务操作入口及 Linux 执行隔离，再扩大客户端和真实工具范围；保持单一恢复负责人，不把实验夹具扩展成另一套自研恢复平台。未发出的 TASK_015 模型设置工作已被替代，此前未提交研究与依赖改动保留，不自动视作已验收或启用。

## 产品与最终架构

交付可自部署的数字同事：保留身份与工作，通过工具完成有界任务，接受监督，安全恢复中断，复用经过审核的方法。
保留一个主仓库：TypeScript/React Web 与薄 Electron 客户端；Python 业务与权限控制层、可独立验证的 Python Agent Runtime；
PostgreSQL 与文件存储；隔离的 Linux 浏览器、工作目录和受限命令环境。优先复用成熟执行后端；Rust 仅在确有宿主需求时引入。

Server 表示职责和权威边界，不表示必须永远使用 TypeScript。迁移期间现有 Server 保持权威，每项迁移职责只有一个选定写入方。
凭据、审批和任务最终提交不交给模型。旧 TypeScript 业务后端是过渡实现，不形成永久双后端。

## 基线与已完成基础

- 迁移分支：`codex/architecture-migration`，`c33e03f1a14de739196113769c59fdaace9029e7`。
- 独立功能分支：`feat/cross-platform-employees`，`9cc73c9e78451e572f57d142d6b9caf62ccb78e2`。
- 已验收：Python SDK 循环、受控子进程、真实 Server/PostgreSQL 确定性流程、显式运行时选择、可选 Python 镜像与锁定依赖。
- 默认仍是 TypeScript。Python 控制层已在显式模式下写入业务数据库，局部生命周期验收截至 `5beed49`；公共派发与生产模型/工具组装仍未完成，现有验证不证明真实模型质量、重启续跑或完整产品。
- 独立功能分支的员工浏览器人工接管和模型连接必须逐项对账，不能将两个副本的能力相加当成一个版本。

## 分段目标

| 阶段 | 交付内容 | 完成依据 | 状态 |
| --- | --- | --- | --- |
| S1 功能与数据对账 | 有源码依据的保留/退役清单、两套 SQL 历史、数据兼容风险、目标接口和状态归属、可恢复源码基线 | 十二类目标均有现状、证据和负责阶段；识别迁移历史冲突；不复制私人数据 | 已完成源码范围对账；未切换数据 |
| S2 Python 控制层与客户端兼容 | 经调研的 FastAPI/Pydantic 参考实现；身份授权、Bot/频道/消息、任务/审批/预算/审计/产物/调度；客户端生成与事件契约 | S2a 身份与授权读取；S2b 单写入方的任务/工具/审批；S2c 设置/文件/调度及客户端兼容，分别验收。无重复派发或生产影子写入 | 进行中：S2a 身份授权基础已本地验收，S2b 任务入队/读取与控制层进程监督已本地验收，持久化生命周期与公开工作参考流程已本地验收；生产派发和客户端兼容仍待完成 |
| S3 持久任务与安全恢复 | 持久状态和检查点、审批等待恢复、取消、未知结果核对、按动作确定重试策略、共享预算 | 在派发/提交/审批前后杀进程重启；保留已完成工作，不盲目重放未知写入；断开客户端不取消任务 | 已选择 Temporal 为目标恢复负责人；参考公开流程、冷恢复、SIGKILL、相邻版本停机升级和定向已关闭历史核查本地通过；生产派发、真实 Runtime 组装、恢复操作和启用门槛未完成 |
| S4 持久执行环境与文件交付 | Linux 浏览器、人工接管、受控自主浏览/填写/上传下载、持久目录、受限命令、隔离代码修改及文档工具 | 真实本地测试站点、独立 Bot 会话、批准写入/接管/取消/重启；文件可打开或渲染；隔离仓库产出通过测试的补丁 | 设计已审查；Linux 部署与执行待完成 |
| S5 记忆、技能与学习评测 | 分范围的相关检索、纠正与支持范围内示范形成候选技能、版本/审核/测试/停用/回滚、独立评测 | 纠正规则审核后用于新任务并显示效果；可撤销；保留来源、范围、删除控制，不扩大权限；保留 Hermes 归因 | 待开始 |
| S6 协作与开放扩展 | 保留现有委派并接入持久任务；共享资源/预算约束；MCP 认证生命周期；保留并迁移按 Bot 模型配置与受控本地端点 | 委派电脑/文件工作仍使用独立授权；冲突/取消验证；连接器刷新/撤销/失败；换模型保留资产；独立模块贡献样例 | 待开始 |
| S7 收敛、迁移与整体验收 | 合成旧数据升级、完整备份恢复、薄 Desktop/Web/手机网页监督、可恢复归档、默认切换与发布准备 | 完整用户旅程、全新检出验证、目标 CI、明确的真实模型评测；生产或对外动作单独列明，不虚报平台支持 | 待开始 |

S2 保留分段验收。根据本次长期产品决策，S3 选型与 S4 执行边界先于 S2 的进一步扩展。S3 的回执和重试语义约束 S4；S2 任务/产物契约稳定后，S5 可以独立推进。
S6 依赖契约，不依赖每一个可选文档处理器。仅在新证据改变依赖时调整顺序，并记录原因。

## 十二类目标覆盖

| 目标 | 负责阶段 |
| --- | --- |
| 01 持久 Bot 身份 | S1 保留范围、S2 迁移、S7 验收 |
| 02 私聊与协作频道 | S2 接口与客户端兼容、S7 验收 |
| 03 多步骤执行 | 已有循环、S2 权威、S3 恢复、S4 真实工具 |
| 04 持久工作环境 | S3 回执、S4 工作目录和浏览器 |
| 05 浏览器与工具操作 | S4 执行、S6 连接器兼容 |
| 06 文件与代码交付 | S2 产物权威、S4 文档工具与工作目录 |
| 07 批准与人工接管 | S2 审批、S3 恢复、S4 控制权 |
| 08 后台、定时与安全恢复 | S2 调度、S3 持久执行、S7 生命周期 |
| 09 分范围的长期记忆 | S5 检索和来源 |
| 10 技能与教学复用 | S5 审核、评测和回滚 |
| 11 有负责人的多 Bot 协作 | S6 委派、共享预算和冲突处理 |
| 12 开放工具、执行后端与模型配置 | S6 兼容和独立贡献 |

目标文件包括文本/代码、Markdown、CSV/JSON、PDF、DOCX、XLSX、PPTX，分别记录读取/提取/创建/修改/渲染能力。
不承诺任意格式无损处理。OCR、转录、图片生成和专业处理器保持可选扩展。

## S1 权威与保留基线

[能力清单](MIGRATION_CAPABILITIES.zh-CN.md)将十二项能力及退役候选映射到源码证据；
[SQL 兼容说明](MIGRATION_DATA_COMPATIBILITY.zh-CN.md)记录分叉历史。
上面的完整提交号是源码恢复引用，不是用户数据备份。原仓库未提交文件和运行数据不在迁移写入范围内。

| 职责 | 当前权威 | 目标权威及门槛 |
| --- | --- | --- |
| Owner 会话、Bot 身份、频道成员、设置 | TypeScript Server 与 PostgreSQL/加密设置 | S2 Python 控制层；兼容 ID/返回格式，显式选择唯一写入方 |
| 任务状态、审批、取消、审计、产物登记 | Server 数据库状态及策略 | S2/S3 Python 控制层；事务、回执、撤权检查 |
| 策略、上下文整理、委派建议、经验提案 | 有界 Runtime 提议，Server 检查副作用 | 可替换 Python Runtime；不拥有凭据及权限策略 |
| 浏览器/目录副作用与人工控制 | 已登记 Worker/后端与 Server 批准 | S4 隔离执行器；范围授权、持久回执、人工否决 |
| 客户端快照及事件 | Server 数据与临时实时通知 | S2 定义版本/游标/重连语义；客户端只显示权威状态 |
| 记忆/技能是否可用 | Owner 审核后的持久事实 | S5 审核、版本、范围约束；Runtime 不能自行批准 |

S1 固定职责归属；精确快照版本和持久游标属于 S2，必须通过重复、乱序与重连夹具验证。

## 验收记录

- S3 产品组合（父提交 `d0f7c1a`）：可选产品 Workflow/Agent、显式 mTLS 单次派发与独立发布核验，
  已通过真实公开 HTTP/PG/mTLS 模型及发布恢复、并发取消、产物回读与无副作用重放。
  产品代码不导入实验目录；服务回调仍是合成夹具。通用继续与产品服务配置尚未完成。
  详见[证据](research/work-product-worker.md)。

- S3 模型回执（`d2b3372`）：可选真实 SDK 端口限制文本／函数输入，在已准入 Action 下持久保存
  原回复与用量。真实公开 HTTP/PG/mTLS 流程通过保存后 Worker 崩溃、claim 过期后的同 Activity
  恢复、唯一结算、产物下载与无副作用回放。丢失／损坏回执不重发，不退未知用量。
  提供方传输为合成夹具；产品配置／继续和 S4 仍未完成。SQL0033 保留旧历史并通过既有 S7
  合成迁移／恢复检查。见[证据与边界](research/work-model-ports.md)。

- Temporal 相邻版本升级：12 项公开工作记录和 57 项参考单元检查通过。经校验的官方 1.31.3
  二进制覆盖在固定 1.32 镜像上，健康运行 601 秒后停机升级到实测 1.32.0；schema 历史、命名空间
  和四个分片保持一致。待批准和已提交未确认任务先在升级后的原卷继续，再验证恢复旧引擎快照仍保留
  较新的业务结果；无重复写入、产物、完成事件或授权复活，未知写入仍需核对回执。11 份历史通过离线
  重放，不兼容命令均被拒绝；仓库完整检查通过。Linux CI 已配置但未远端运行。此处只验证 arm64
  停机服务升级；两个版本的 PG schema 相同，不代表 DDL 迁移、滚动/高可用、任意回退或 Linux 隔离。
  见[升级证据](research/temporal-release-upgrade.md)。

- 受信引擎传输与离线重放：38 项参考单元检查、八项 mTLS/PG 公开流程本地通过；明文、无证书、未知/退役 CA 与错误名称均被拒绝，合法连接在拒绝前后正常。停机更换 CA 后保留等待任务与 namespace。真实等待/完成历史重放不改变状态或动作计数，首个命令不兼容能被识别；缺少 TLS 路径/配置时先拒绝启动。CI 改为 mTLS 路径，尚未运行远程 CI。这只覆盖一组受信客户端，不是 API 分权、完整 PKI、跨版本升级或不可信 Runtime 隔离。见[传输](research/temporal-transport-security.md)与[重放](research/temporal-history-replay.md)。整体迁移继续进行。
- 发行版 Temporal/PostgreSQL：Server1.32.0、PostgreSQL17.11、历史 schema1.19/可见性1.14 上八项公开流程通过。冷备份恢复到新卷保留 namespace，更新的业务未知写入强制读取回执，仍为五次 POST、一次写入、11 假定用量；引擎与数据库 SIGKILL 后保留审批/取消。缺失/过旧 schema 拒绝启动；维护拒绝对已有库初始化或使用较新版本；运行账户不能 DDL 或修改版本记录。26 项参考单元测试通过，PG 流程已接入 Linux CI。同版本维护不等于版本升级验收；下述传输/重放证据补充此记录，完整产品恢复和资源测量仍未完成。见[运行配置](../deploy/temporal/README.zh-CN.md)。
- Linux 隔离设计已审查固定 Docker/runsc、仅命令隔离及独立浏览器配置，尚未部署或验收。H2 租约向 Python work/helper 权限的映射、写入时容量限制、真实 Linux 对抗检查明确列为缺口。见[有来源的设计](research/linux-execution-boundary.md)。

- 公开工作恢复：五项完整流程加三项引擎身份冲突通过。真实 HTTP/PG 的 Task 提交、持久交接、无 Worker 时批准、API 重连、丢失写入回执后的核对，以及真实产物下载已接成一条流程。写入仅发送一次；未知结果时取消仍记录过去的副作用，不新增最终模型或产物；损坏回执保持未决；发布确认丢失不重复完成。新增 22 项交接数据库测试，真实 PG/HTTP 共 141 项通过；另有精确回执与恢复竞争的定向参考测试。脚本模型/假 HTTP 服务和开发版 Temporal SQLite 不等于生产或 Linux 隔离验收。见[可运行证据与剩余门槛](../experiments/work-journey/README.zh-CN.md)。

- 官方 SDK 组合：八项真实杀进程实验通过。DBOS 普通函数在恢复时重复读取，显式步骤与 Temporal 活动保留已完成结果；无 Worker 期间的决定可恢复，恢复前撤权阻止写入，已完成 ID 不新增操作。实际中断请求尝试多于 SDK 已完成计数；DBOS portable 工作流不会自动改变 SDK 模型步骤的原生存储。后续公开工作参考流程已采用 Temporal 与受信工作流策略，控制层继续拥有权限/预算/产物终态；尚未选定生产引擎或接入现有 stdin/stdout Runtime。见[证据与决定](research/sdk-durability-integration.md)。

- 工作成果发布：新增 `0028` 迁移、单调执行版本、有约束的最终提交和认证后核验真实内容的 Artifact 下载。真实 HTTP 进程重启后仍保留待审批与任务状态；文件/审计失败、写文件期间取消、执行者替换和期限失效均不能错误发布成功。真实数据库/HTTP 检查 119 项、Python 包检查 810 项及 `npm run check` 通过，数据库用例单独运行。当前确定性夹具尚未接入引擎或外部工具，本地 POSIX 证据不等于生产存储或 Linux 隔离验收。见[发布研究](research/work-artifact-publication.md)。

- Runtime 延续：14 项固定 SDK 实验经独立新进程复跑通过；已完成读取只执行一次，恢复仅新增一次模型调用，请求计数从 2 延续至 3。非法延续另验证不得先新增模型/工具调用。SDK 会接受格式正确但内容改写的历史，来源仍须由控制层保证；官方细粒度耐久适配器继续保留为候选，SDK ID 不替代业务 Run。Python 包检查通过 788 项，105 项数据库用例单独运行；完整仓库检查通过。

- 工作领域基础：新增 `0027` 迁移，保存独立 Task/Run、原子待派发记录、不可变 Action、绑定内容与期限的审批、Task 共享预算预留及带版本事件。新增 10 项真实 PostgreSQL 检查通过（控制层集成共 105 项），覆盖并发提交/预留、冲突审批、撤权、未知结果下的取消、真实超额用量和事务回滚。ASGI 客户端关闭重开后可读同一已提交快照；这不等于浏览器/SSE 重连或真实 Worker 恢复。该检查点尚无引擎接线、产物完成路径或公开核对接口；上面的发布工作只补上了产物完成路径。见[研究与边界](research/work-domain-admission.md)。

- 最新方向首轮实验：[10 项 DBOS 本地故障场景](../experiments/durable-execution/README.zh-CN.md)通过，真实杀进程/重启、临时 PostgreSQL 和独立 HTTP 写入计数。反例复现无保护的重复写入；意图保护会停在未知结果。批准跨进程保存，撤权阻止动作，取消保留已发生的副作用。这是候选证据，不等于 S3 完成或生产引擎选定；任务领域模型及客户端完整流程仍需实现。


- S1：17 条相同 SQL 后出现两条时间戳/哈希冲突；已提交源码预检可拒绝分叉及非法输入，现有启动迁移保护不变。
  没有检查或修改已有数据库。
- S2a-1/2：独立 Python 读取与显式 Owner 登录/退出、OpenAPI、精确历史保护、持久限流已实现。
  198 项本地测试与 9 项自有 PostgreSQL/真实 HTTP 检查通过，包括 TS/Python 双向会话与撤权、失败回滚。
  已独立验收 DeepSeek 的 128 项投影测试，记录更严格的异常旧数据处理并在真实数据库验证。默认后端未切换。
- S2a-3：显式 Bot/频道创建和身份/成员/审计原子事务已实现，合计 19 项 PostgreSQL/HTTP 检查通过，
  包括重名、部分失败回滚、等锁撤权、提交前过期及 TS 回读。工人的输入与生成 schema 已独立验收：253 项包测试和 81 项真实 Zod/Python 差分通过，
  另有 19 项真实数据库/HTTP 检查。源码声明随 Python 包保留。
  依据已安装 Zod 的真实行为，将早期认证长度修正为 Unicode 字符计数。
- S2a-4：私聊单例创建与普通频道幂等加入已验收，和身份创建共用 Owner 事务。
  275 项包测试、24 项合计真实数据库/HTTP 检查通过，保留 TS 回读与 81 项输入差分。
  已验证并发、精确审计、回滚及异常已有成员拒绝。
- S2a-5：认证后的最新 100 条消息与真实 TS 夹具和回环 HTTP 对照通过。
  最终包包含 288 项测试；29 项合计真实数据库/HTTP 与 81 项输入差分通过。
  文本传输和 JSON 分别限制 4 MiB，覆盖并列排序与读途中撤权；未启用消息派发。
- S2a-6：Owner 资料编辑带版本冲突检查，Bot 版本、成长事件和审计一起提交。
  最终 316 项包测试、35 项真实 PostgreSQL/HTTP、105 项实际 Zod/Python 输入对照通过，覆盖并发编辑、回滚、过期/撤权及 TS 资料回读。
  HTTP 预检查已复用，数据库仍保留会话锁和提交前检查。
- S2b-1：显式任务原子入队与最新 50 项任务读取已本地验收。345 项包测试、45 项真实 PostgreSQL/HTTP、129 项实际输入对照及 60 项实际 TS/Python 路由/任务投影对照通过。
  覆盖精确审计、多接收者整体回滚、同频道回复、私聊范围、并发时间与 TS 回读。DeepSeek 负责输入/路由/投影，主控实现 SQL/HTTP 并独立验收。
  附件引用在 S2c 文件权限迁移前明确拒绝；尚未接入任务执行器。
- S2b-2 控制层与进程边界：651 项本地包测试通过，包含 83 项进程/真实 SDK 测试及 49 项宿主权限测试；48 项实际 TS/Python 协议对照通过，另有 45 项数据库测试独立通过。
  DeepSeek 提供协议/进程初稿及故障夹具，主控实现宿主并通过独立回归修复取消与启动时 PID 所有权。未改变凭据或默认后端。
  Linux 环境验收记录见[监督层证据](research/python-control-runtime-supervision.md)。
- S2a 身份授权基础流程本地验收通过。后续 S2b 工具/模型/审批和派发工作先经过上文恢复与领域决策；旧的“下一步”安排被替代，已有[权限审查](research/python-task-authority.md)保留。
  完整资料读取聚合任务、审批、产物、技能和记忆，随 S2c 客户端兼容完成，不能用空数据冒充。S2b-1 已保留消息与任务的原子提交，移除成员仍随 S2b 取消权限迁移：
  现有源码要求消息与任务一起创建，移除成员同时取消任务/审批，不能提前拆成只改聊天数据的写入。实时事件与客户端兼容仍未完成。
- 已接入现有 Python Linux CI；本地尚未推送的修改没有托管 CI 运行结果。完整检查与提交引用在集成时记录。

## 分工与验收

主控直接实现架构、公共契约、权限/数据/生命周期关键代码并独立验收；一个辅助实现者每次只负责明确文件范围的一张任务卡。
使用短任务单与文件交接，减少重复上下文和已通过检查。并行工作不得同时修改相同文件。
不能仅凭辅助模型的总结验收；必须检查差异、运行相关验证。行为修改前记录固定版本的一手复用证据，公共文档同步中英文；集成前运行仓库要求的完整检查。

## 保留与退役

保留 Bot 配置、对话、任务、文件、审核知识/技能、审批、取消、审计、调度、模型配置和有用的导入导出。
候选退役项包括 Swift/C# 宿主探索线及被替代的安装链、办公室可视化、未实现 Provider 占位、被替代的 TypeScript 后端。
凭据保护、授权、许可证、迁移历史和有效安全测试不能随外围功能一起删除。

每项退役必须有能力决定、依赖/构建/CI 影响、替代验收、源码恢复引用及数据路径。不删除私人数据或未提交工作。
原生手机应用、全系统本机控制、员工交易、完整插件商店、企业多租户计费、自主模型训练不计入本次完成范围。

## 整体完成标准

用户创建 Bot，上传表格，在测试业务网站核对记录，委派独立核查，批准具体外部修改，关闭再打开客户端，经历人为服务中断，
获得可使用的结果文件，并将一条纠正审核为可复用方法后用于新输入。另需证明换模型保留资产、撤权不可绕过、合成旧数据可升级和恢复。
区分确定性/本地证据与真实外部服务证据。

新增目录、翻译代码、通过模拟测试或删除旧文件都不单独代表完成。必需开发仍未完成时总目标保持进行中。
发布、生产变更和不可逆清理，应先准备具体可审阅结果，并按适用授权执行。


## S2b Owner 任务命令进展

显式 tasks 参照模式已实现 cancel/steer：取消原子更新原生目标和符合范围的活动子任务；补充指令与完成
事务共用 Run 行锁，每个任务最多八条。71 项组合 PostgreSQL/HTTP 检查已通过，覆盖真实命令写入者并发、
审计回滚、等待行锁期间会话过期、Node/执行环境拒绝、容量限制，以及 TS 对取消与补充指令的读回。
这是已持久化的命令权限；进程/插件通知、执行调度、最终交付和实时事件仍未完成，不能算整个 S2b 完成。

本阶段最终 Python 包 728 项通过，71 项夹具专用测试已另行完成真实数据库验收，全仓 npm check 通过。
首轮出现两项旧进程时序测试失败，未修改旧实现或超时；独立复跑及随后完整包均通过，过程如实记入研究文件。

DeepSeek 提供输入/投影模块及聚焦测试，Codex 实现事务、接口、真实验收与最终差分脚本；34 项实际 Zod
输入对照全部一致。协作任务已停止并交接，整体迁移目标继续保持 active。


## S2b 持久化执行进展

95 项组合 PostgreSQL/HTTP/SDK 验收已通过，含新增 24 项领取、上下文、用量、失败、交付与真实 Worker 测试。
原子交付保留文件、带版本检查的记忆/技能、待审核经验提案和 Owner 纠正。真实 TS 已回读结果与上下文，
一次性文件的摘要与已提交元数据一致。DeepSeek 提供纯值模块、聚焦测试和 40 项实际 TS 对照；Codex 实现
数据库事务、集成验收并加固深层元数据校验。公开派发与生产工具/模型/审批服务组合仍待完成，整体目标继续进行。

本次最终验收：766 项包测试通过；95 项夹具专用测试在真实 PostgreSQL/SDK 环境中单独通过；全仓 `npm run check` 通过。

WorkBuddy 的[Temporal 源码审查](research/temporal-durability-review.md)已独立核验并纠正业务/引擎身份和取消边界；随后在固定 SDK/CLI/Server 与开发 SQLite 历史配置下通过 [12 项本地故障用例](../experiments/durable-execution/README.zh-CN.md#temporal-实测结果)。显式屏蔽线程取消的动作场景与首轮失败均已记录；这不代表生产部署已验收，仍未选定引擎。
