# Product command qualification

2026-09-25, before implementing the qualification fixture. Reuse the exact Work, command v2,
Node WebSocket/Unix transport, protected Host and Temporal entries in OPEN_SOURCE_REUSE and
work-command-transactions.md, work-command-transport.md and command-protected-host.md.
The missing evidence is their composition at the real serve.py entry, with an Owner HTTP
submission and approval, one command observation, full output publication and separate review.

Use the existing owned PostgreSQL/mTLS Temporal lifecycle fixtures and synthetic model HTTP
transport. Use the actual OpenBotNodeClient with an explicit empty legacy Provider list and the
existing unixCommandInstallation factory. No alternative Node protocol or execution endpoint is
introduced. Local development uses an explicitly synthetic Native behind the actual Host and
real Unix/WebSocket transports; this cannot establish Linux execution or isolation support.
The subsequent Linux case must use the protected Native and its original Invocation cleanup.

All inputs are synthetic CSV, model credentials and ephemeral test identities. Control private
keys, Owner cookies and PostgreSQL/Temporal credentials stay on the Control machine. A remote
case must have a concrete upload manifest and an existing authorized disposable test window.
No new dependency, copied upstream source, default feature activation or production route is
part of this fixture. Installed esbuild 0.28.2 is a development-only packaging tool; its output
must retain bundled dependency notices and exact input hashes if uploaded.

## Local product checkpoint

The canonical43 journey passed after the Owner Docker model-selection correction and trusted
command claim correction documented in their existing research records. Real Owner creation,
attachment upload, Node enrollment, command approval, Work admission, signed Host observations,
private output storage, independent content review, both artifact downloads and offline replay
passed. The single original command was not repeated during replay. The fixture's owned Control
and engine resources closed and its local Control private key was removed. Public evidence is
`experiments/work-journey/evidence/product-command-local.json`; synthetic Native/peer/model
boundaries remain explicit. This result does not qualify remote Linux or browser execution.
