# 独立开发主线

[English](DEVELOPMENT_TRACKS.md) · [简体中文](DEVELOPMENT_TRACKS.zh-CN.md)

这是 2026-09-22 方向的当前执行入口：无界面且可独立验证的 Agent 服务、共享官方客户端、可独立
维护的扩展。保留单仓库与现有技术栈；身份、权限、批准、审计和持久任务状态仍由 Server 掌握。
模块边界不能授予权限，也不能代替沙箱。

开发基线是 `main` 的 `ebce995`，不是旧工作目录的 `9cc73c9`。本文接替历史八小时计划的后续排期，
不改写历史阶段编号，也不表示整个产品路线已经完成。

## 主线与集成责任

| 主线 | 模块入口 | 基础工作包 | 验收结果 |
| --- | --- | --- | --- |
| R：Runtime 与无界面交付 | Server native-agent、PostgresAgentStore | R1：确定性 Server 旅程，续跑与可选学习回归 | 已鉴权任务完成并能下载报告；失败、取消、晚到结果、客户端断开均可验证，无需 Web、Electron 或付费推理 |
| C：客户端与状态契约 | ControlPlaneStore、workspace API、domain、只读示例 | C1：一致快照与明确重同步 | 持久字段和计数来自同一数据库快照；独立只读客户端处理重复、旧帧和重连；明确分页范围 |
| P：扩展兼容性 | Server 插件、独立 MCP 脚手架 | P1：共享预检与可理解的失败原因 | 授权前拒绝不支持的要求；独立成功/schema/认证/超时用例不依赖 OpenBot workspace |
| D：贡献者验证 | scripts、现有 CI、贡献指南 | D1：影子模式影响分析 | 复用 Turbo 官方依赖图；高风险、未知或失败自动建议全量；不跳过既有必需检查 |

每个工作包使用独立分支和工作区，由一个实现任务负责代码、回归测试、固定版本研究和中英模块
文档。共享协议、存储、安全路径先统一，再让客户端并行接入。集成任务负责公共清单、复用账本、
完整检查和跨主线回归；不虚构模块维护者或发布负责人。

后续新实现优先交给已有 Grok Bot 协作者，Codex 负责有界审查、集成和验收，不再安排 Codex
子 Agent 重复实现同一任务。Grok Bots 共用机器：每包必须有独立 Git worktree 和测试数据库，
命令指定工作目录，推送前核对提交与目标分支；只有分支名并不能隔离工作区。交付提交或补丁、
实际检查和剩余限制，集成审查前保留 Draft PR。尚未测量 token 节省比例。

## 首批完成记录

| 工作包 | 状态 | 证据入口 |
| --- | --- | --- |
| R1 | 已完成本地集成 | `npm run test:runtime`：90 项通过；[Native Agent](NATIVE_AGENT.zh-CN.md) |
| C1 | 已完成本地集成 | [契约](WORKSPACE_SYNC.zh-CN.md)、[研究](research/workspace-snapshot-stream.md)、HTTP/订阅/客户端测试与真实 PostgreSQL 并发交错测试 |
| P1 | 已完成本地集成 | [MCP 作者契约](PLUGINS.zh-CN.md)、13 个独立场景、固定诊断提示及 Server 回归 |
| D1 | 已完成本地集成 | [影子报告](CI_IMPACT.zh-CN.md)、真实 Turbo 夹具及 CI 契约测试 |

集成后必须运行 `npm run check`、冷启动无界面旅程和明确的临时数据库验证。确定性模型证明执行
流程，不证明真实模型质量；本地验证不代表远端 CI 或原生平台支持。只有具名门槛通过后才更新状态。

## 第二批实现与定向证据

各工作包基于 `2cc32d0` 使用独立分支；统一集成分支为 `codex/collaborative-development`，
原始工作目录保持不动。

| 工作包 | 独立分支 | 状态与验收入口 |
| --- | --- | --- |
| R2：Runtime 接口 | `codex/track-runtime-ports` | 已完成本地集成，整仓检查通过。`npm run test:runtime:unit`：22 项；`npm run test:runtime`：112 项。Server 内执行单元有明确权限、模型、工具、存储、审计接口，不宣称已发布独立包或支持崩溃检查点。 |
| C2：共享客户端夹具 | `codex/track-client-fixtures` | 已完成本地集成，整仓检查通过。六种正式组件场景、1440px/390px 真实浏览器、合成 Blob 下载与重连补读。见[客户端夹具](CLIENT_FIXTURES.zh-CN.md)。 |
| D2：贡献者冷启动 | `codex/track-contributor-journey` | 已完成本地集成，整仓检查通过。真实 Owner 登录、Node 登记、拒绝令牌复用、保留 Session/身份重启和中断清理。见[贡献者冷启动](CONTRIBUTOR_JOURNEY.zh-CN.md)。 |
| O1a：保留旧数据升级 | `codex/track-retained-upgrade` | 已完成本地集成，整仓检查通过。`npm run test:upgrade`：固定旧前缀、15 张表、生产入口并发/重复迁移、旧列一致、约束、安全默认值与漂移检查。见[数据库指南](DATABASE.zh-CN.md#保留旧数据的升级回归)。 |

冷启动 smoke 必须先于其他生成构建产物的命令。客户端夹具和 Runtime 单元检查不需要数据库；
无界面验收自动管理临时容器；升级验收显式指定独立空白本机库。这些入口各自验证不同范围。

## 后续工作包与依赖

下文第三批已完成集成。C4、B1b、F1a 和 Grok 的 D3 已合入本地分支，组合检查通过。
Grok 的 R3 修订也已合入，D4 仍在实现；P2 已有研究交接，尚未启用 OAuth 功能或真实服务。

| 工作包 | 下一项具体成果 | 依赖和边界 |
| --- | --- | --- |
| D4：入门 DOM 回归 | 实现四张对话框/Tab 的定向回归卡 | Grok Builder；依赖 D3 [Draft PR #85](https://github.com/Peerframe/openbot/pull/85)，使用独立工作区；jsdom 不证明真实浏览器无障碍。 |
| P2：账户生命周期 | 一个经审核 OAuth 连接器完成登录、过期、刷新、撤销及有限诊断 | MCP 授权研究与明确测试账户；禁止凭据透传和静默扩大权限 |
| F1b：经过测试的代码交付 | 合成输入生成通过测试、产物可追溯的补丁 | F1a 记录报告输入，不代表结论已验证或代码已测试。实现前明确代码执行边界，复用文件/Worker 机制。 |
| S1b：人工核对与回执保留 | Owner 可记录核对后的外部证据并管理回执容量 | S1a 保留未知结果；收到答复或取消任务不证明外部动作完成或回滚，禁止重放不确定写入。 |
| L1：可验证学习 | 审核过的纠正形成有版本、证据、回滚的方法 | R1 保证学习可选；保留 Hermes 归属，区分检查点、记忆、轨迹和技能 |
| M1：有用协作 | 有证据交接、明确主负责人、共同预算和资源互斥的委派 | 稳定任务/动作契约；不同 Bot 名称不代表凭据隔离 |
| O1：剩余运维与兼容 | 运维备份适配器、其他恢复配置与明确发布责任 | O1a 验证旧数据升级，O1b 验证 POSIX Server 模型目录模式的合成配套恢复；签名、旧版与 Desktop 密钥配置、归档加密、异地保留和 PITR 仍未完成。 |

以上是待开发工作，不是已交付功能。每项先定义有界验收旅程并找到复用账本，按 `AGENTS.md`
检查上游源码、发布、测试、issue 和许可后改变行为。权限、存储、外部动作继续失败时拒绝。
原生移动客户端、企业多租户权限、全平台电脑操作、扩展商业市场、所有权转移和办公室插件均不是
首批成果；它们仍在产品路线中显式保留，不能默认承诺已经支持。

## 贡献交接

S1a 已有独立、有界的持久回执账本；500 条审计与临时批准参数不是恢复检查点。未知回执不会
随历史淘汰，256 条受保护记录占满时拒绝新调用。B1a 已通过官方 runner 验证受审浏览器点击，
但两者都不等于整条主线完成。B1b 已补真实 Owner Worker 取消和中断时未决批准失效，统一
Run 先于审批的加锁顺序，拒绝晚到结果，并在 Node 清理完成前保留占用；仍不证明远端回滚。能力租约 ADR
仍是提案，不能据此宣称已有跨进程互斥。

可领取任务必须明确结果、非目标、入口、依赖、定向命令、失败用例和审查入口。尚未解决的架构题
不是新手任务。使用仓库测试与合成数据，不依赖私人聊天。成本分别记录开发 AI、产品推理、执行/CI、
维护者时间；没有实际计时和比较证据时，不声称影子报告已经降低了成本。

## 2026-09-22 集成证据

- 四条主线合并后，`npm run check` 本地通过，包含 Server/Web/Desktop 类型检查、测试及构建；
  原有需要特定环境的跳过项仍明确保留。
- `npm run test:runtime` 使用新建并自动清理的 PostgreSQL 容器、真实 Owner API 和确定性模型，
  90 项测试通过。
- `npm run test:workspace` 的 Server/订阅/读取器共 64 项通过；最终独立消费者另有 3 个 Node
  场景通过，包含代理静默断线超时。
- `npm run db:verify` 在本任务独立合成数据库中通过，覆盖迁移校验和在工作区列表、计数读取
  之间强制提交并发变更。
- 真实浏览器使用独立 Vite 入口、合成 Owner 与本次 Server 构建，已验证登录、完整计数显示，
  停止上游后页面明确显示 stale；Server 重启后无需重新登录，自动恢复 current。
- 独立复核覆盖 Runtime 改动、MCP 生产打包边界及快照取消/新鲜度；最终取消测试无需手动
  完成阻塞读取，也能让订阅退出。

CI 已在现有临时数据库 job 接入同一无界面旅程，并由 `db:verify` 执行快照验证；影响报告保留
全量门槛。本批没有验证远端 hosted CI、原生安装、真实付费模型质量或外部 OAuth 账户，也没有
发布版本。上面的后续工作包仍待开发。

## 第二批集成证据

- R2、C2、D2、O1a 合并后，完整 `npm run check` 通过。CI 另构建独立客户端夹具；普通
  Web/Desktop 生产入口不包含该场景包。
- 集成分支的 `npm run test:runtime` 共 112 项确定性与真实 PostgreSQL 测试全部通过。
- PostgreSQL 17.11 旧数据升级验证通过：15 张表、27 条迁移。已有数据、仅含函数、非系统
  schema 的数据库均会被拒绝且不应用迁移；缺少指定样本时在连接前明确失败。
- 独立复核没有发现 Runtime 权限/取消退化或升级生命周期阻塞问题。六个客户端场景经过
  桌面和窄屏真实浏览器验证，没有真实 API 请求或控制台/页面错误。预览采用构建后刷新，无 HMR。
- CI 在原有构建前 smoke 加入 Node 保留身份重启，并使用单独升级测试库；原有必需检查保持。
  本地证据不代表远端 CI、原生安装或真实付费模型效果。
- 最终冷启动复验发现 Darwin 僵尸进程组返回 `EPERM` 的竞态，已修正为有界核对进程身份/状态，
  保留真实权限失败并允许清理重试。10 项辅助回归通过；重新导出的集成代码完成全部登录、登记和
  身份重启流程，未残留自有进程、端口、容器或私有夹具目录。此结果替代此前失败的冷启动尝试。

## 第三批完成记录与集成证据 — 2026-09-23

| 工作包 | 结果 | 验收与边界 |
| --- | --- | --- |
| C3：权威计数 | 已完成本地集成 | 全局活跃数由 Server 补读，不再根据最近 50 条任务计算。重复、未加载任务事件和并发事件流不会累积虚假增量；hook/App 回归覆盖有界合并、错误与清理。 |
| S1a：持久调用回执 | 已完成本地集成 | 独立加密账本最多 256 条，保留批准、派发和答复事实。真实进程强杀验证派发前零调用、丢失响应后一次调用且结果未知，两次重启均不重放。任务详情通过有界读取展示回执；未知结果受保护，人工核对与容量管理留在 S1b。 |
| B1a：Docker 浏览器生命周期 | 已完成本地集成 | `npm run test:provider:docker -- --output <新报告.json>` 的 15 项必需检查全部通过，零失败、零跳过。使用生产 Server/Node/Provider、真实 PostgreSQL 和合成电脑 HTTP 服务，属于隔离集成证据，不是实际浏览器或原生平台认证。 |
| O1b：配套恢复 | 合成 POSIX Server 模型目录配置已完成 | `npm run test:restore` 通过：25 张表、8 个文件、27 次迁移；真实 Owner 下载、解密、保留未知回执、缺失/错误密钥、文件篡改和截断 dump 的事务回滚。其他密钥配置和运维备份仍待开发。 |

- 最终集成 `npm run check` 通过：Server 562 项（75 项依赖特定环境而跳过）、Web 394 项、
  Desktop 359 项（1 项既有跳过）、Node 51 项（3 项既有跳过），31 个测试任务及 18 个构建任务。
  独立真实 PostgreSQL 无界面旅程的 112 项全部通过。
- 浏览器驱动的 12 项定向夹具/进程回归通过。审查发现只停止直接子进程会留下存活孙进程，现已
  与贡献者 smoke 共用 POSIX 进程组清理，覆盖超时、中断、父进程先退出、继承管道及无关进程保留。
- 从集成提交 `173e95f` 全新导出的目录，仅安装锁定依赖后，即通过 Owner 登录、一次性 Node
  登记和 Server/Node 保留身份重启。没有预构建或私人配置；验收后已清理本次容器、进程、文件及
  导出副本。原始 `9cc73c9` 工作区和未跟踪文件保持不动。
- 第七个 `plugin-receipts` 客户端场景通过真实桌面/窄屏检查，原六场景保持可用。集成预览中，
  未知结果提示位于重新提交按钮前，已终结回执默认折叠；生产构建不包含夹具。
- CI 在现有数据库 job 加入必需恢复与 Docker 验收并保留报告，尚未观察 hosted 执行。本批没有
  发布版本，也不宣称验证了真实付费模型、外部 OAuth 账户或新增原生安装。

## 第四批集成与 Grok 交接 — 2026-09-23

| 工作包 | 结果 | 证据与边界 |
| --- | --- | --- |
| C4 | 已合入本地 | 正式 Web/Desktop 接入快照，使用操作/GET 顺序屏障、旧帧拒绝和独立 Desktop 连接槽。七项真实浏览器夹具通过，含 390px 窄屏；持续旧事件仍可能每秒触发一次合并 GET。 |
| B1b | 已合入本地 | Owner 取消 Worker 后未决批准失效、晚到结果被拒绝。真实测试发现并修复成员移除与审批的死锁，采用兼容的频道行锁；包内 13 项 PostgreSQL 事务及 18 项 conformance 检查通过，取消不证明远端回滚。 |
| F1a | 已合入本地 | 报告附录和产物元数据记录原文件/文本 SHA-256、累计 UTF-16 读取范围和提取截断。六项证据测试及真实 Owner 上传→纠正续跑→下载报告通过，不代表结论已验证或交付代码已测试。 |
| D3 | Grok 交付已合入本地 | [Draft PR #85](https://github.com/Peerframe/openbot/pull/85)，源提交 `7b9b930`：四张小 DOM 回归卡和大型工具任务重新分级；具体实现等待 D4 验收。 |
| R3 | Grok 修订已合入本地 | [Draft PR #86](https://github.com/Peerframe/openbot/pull/86)，源提交 `2ceb806` / `6815464`：SQL 保留 PostgreSQL 微秒；固定时间轴证明旧实现失败，纳入前序答复并排除后续人类/独立任务树/开始后输入。 |
| P2 研究 | 已合入，实现待开发 | [OAuth 生命周期](research/mcp-oauth-account-lifecycle.zh-CN.md) 明确严格发现、刷新/断开竞态、禁止重放及合成验收旅程；尚未新增依赖或真实账户。 |

组合后的 `npm run check` 在 `6ff3f48` 通过：Server 574 项（89 项环境相关跳过）、Web 421 项、
Desktop 365 项（1 项既有跳过）、Node 52 项（3 项既有跳过）、31 个测试任务及 18 个构建任务。
普通门槛不会执行跳过的数据库套件。F1a 独立 Runtime 的七个无界面场景通过，但既有前序答复上下文
用例失败，总计 112 项通过、1 项失败。合入 R3 后，`npm run test:runtime` 在新建 PostgreSQL
夹具上 113 项全部通过，最终 `npm run check` 也通过。集成后的 Worker 套件通过 13 项数据库事务
测试及 18 项必需 conformance 检查，零失败、零跳过，自有资源已清理。D4 和剩余主线仍未完成。

对 Grok 首版 R3 的审查发现固定答复/开始时间与当前时钟的输入时间混用，修订已固定完整时间轴并
证明旧实现失败、修复后通过。集成时将独立任务树反例放在开始之前，确保只能由输入边界排除，
而不能借开始时间边界通过测试。Grok 共用工作目录还造成另一个 Bot 切换分支时干扰推送，误推并删除
了一个空 D4 远程分支，未合并 `main`。后续必须使用独立 worktree 和明确提交到目标分支的推送。
Codex 不重复实现 D4/R3，负责审查与集成检查。
