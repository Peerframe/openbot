# Server deployment

The default image is the Python product. See the [canonical deployment guide](../deploy/server/README.md) for fresh-project deployment, immutable dependency pins, persistence and qualification. Legacy TS container entry points are retired; existing data volumes are retained and require an explicit migration.

[简体中文](SERVER_CONTAINER.zh-CN.md)
